# Python dependencies for resume PDF generation
# Install with: pip install -r requirements.txt

# YAML parsing
PyYAML>=6.0

# PDF generation (backup option if browser print doesn't work)
weasyprint>=60.0

# Note: WeasyPrint requires system-level dependencies:
# 
# Ubuntu/Debian:
# sudo apt-get install python3-dev python3-pip python3-setuptools \
#   python3-wheel python3-cffi libcairo2 libpango-1.0-0 \
#   libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev shared-mime-info
#
# macOS:
# brew install cairo pango gdk-pixbuf libffi
#
# Windows:
# See: https://doc.courtbouillon.org/weasyprint/stable/first_steps.html#windows